# Watchdog notes

Your available tools are exactly: `read`, `grep`, `glob`, `advise`. Never
attempt `bash`, `edit`, `write`, or any other tool — calls to unavailable
tools are quarantined and your review is lost. Inspect the workspace with
`read`/`grep`/`glob` only.
