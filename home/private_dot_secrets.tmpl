#!/usr/bin/env bash
# Secrets managed by Bitwarden via chezmoi.
#
# To enable:
#   1. Store your secrets as a Bitwarden Secure Note named "dotfiles/secrets"
#      with export KEY=VALUE lines in the Notes field
#   2. Log in: bw login && export BW_SESSION=$(bw unlock --raw)
#   3. Replace this file's content with:
#      {{"{{"}} (bitwarden "item" "dotfiles/secrets").notes {{"}}"}}
#   4. Run: chezmoi apply
#
# For now, source your local ~/.secrets manually after first setup.
