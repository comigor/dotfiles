#!/usr/bin/env bash
# Frontier config managed by Bitwarden via chezmoi.
#
# To enable:
#   1. Store the full content as a Bitwarden Secure Note named "dotfiles/frontierrc"
#   2. Log in: bw login && export BW_SESSION=$(bw unlock --raw)
#   3. Replace this file's content with:
#      {{"{{"}} (bitwarden "item" "dotfiles/frontierrc").notes {{"}}"}}
#   4. Run: chezmoi apply
#
# For now, source your local ~/.frontierrc manually after first setup.
